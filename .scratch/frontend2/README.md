# frontend2

Chartered 2026-08-25 by Ross from `/Users/ross/vault/raincheck-runbook/DESTINATION.md`
(the four products P1-P4) and its planning output `DESTINATION-PLAN.md` §2.

Build-only: there is no wayfinder map for this effort and none is owed. The ticket files
under `issues/` ARE the specs — each was written from its own paste box by the session
that ran it, and a measurement that contradicts one supersedes it (correct the file and
its runbook summary line in the same commit).

Numbering is `NN-<slug>.md`, Status on line 3, and the runbook
(`waves/wave-N.md` + `RUN-LOG.md`) is the only coordination surface.
